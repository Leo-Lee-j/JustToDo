# JustToDo PRD 开发就绪度评审报告

| 项目 | 内容 |
|------|------|
| 评审对象 | `JustToDo-PRD.md` |
| 评审日期 | 2026/08/20 |
| 评审模式 | THOROUGH → ADVERSARIAL |
| 评审前版本 | v1.1 |
| 评审结论 | **NO-GO（需返工）** → 修复后 **GO** |
| 修复后版本 | v1.2 |

---

## 一、总体结论

**评审前（v1.1）**：**NO-GO**

PRD 在三个 P0 核心特性上存在技术方案错误或未定义（任务栏钉住、多窗口状态同步、窗口初始位置自相矛盾），且存在大量跨章节不一致与未定义概念，开发者在第二个工作日就会卡住。综合分 **4.2 / 10**。

**修复后（v1.2）**：**GO**

已修复全部 5 个阻断项 + 8 个重要项 + 次要项，文档达到可启动开发标准。

---

## 二、阻断性问题（Blockers，已全部修复）

### B1. 任务栏钉住条的可行性与定义均不成立 ✅ 已修复
- **问题**：3.1 声称"支持在任务栏任意位置钉住"，但 Windows 任务栏是系统外壳区域，第三方窗口无法真正钉入。9.3 方案只解决"定位"未解决"钉住"语义；6.2 引入未定义的"吸附"概念。
- **修复**：重定义为"紧贴任务栏上边缘的置顶无边框浮窗"（非嵌入）；补充左/中/右定位算法、多显示器兜底、Z-order 处理、任务栏自动隐藏时的托盘降级。
- **位置**：3.1、6.2、9.3、13.1

### B2. 多窗口状态同步方案在技术上不成立 ✅ 已修复
- **问题**：13.1 给出"统一 Pinia store + Rust 单一数据源"，但 Tauri 每窗口是独立 webview，Pinia 无法跨窗口共享。前端同步机制完全缺失。
- **修复**：新增 9.4 架构——Rust 侧单一数据源 + Tauri `emit`/`listen` 事件总线；定义 7 类事件协议；写冲突用乐观锁；60s 全量刷新兜底。修正 13.1 错误表述。
- **位置**：5.2、9.4、13.1

### B3. 窗口初始位置自相矛盾 ✅ 已修复
- **问题**：3.1"启动后默认最小化到桌面左上角" vs 6.1"初始位置：桌面右上角"。且"最小化到桌面左上角"自相矛盾。
- **修复**：统一为"右上角，距顶 20px 距右 20px，可拖拽"；明确"关闭窗口=隐藏到托盘，非退出"。
- **位置**：3.1、6.1

### B4. "自动隐藏"行为未定义且引入未定义概念 ✅ 已修复
- **问题**：3.1 混淆定时隐藏与鼠标离开隐藏两种机制；6.1 引入未定义的"半透明小条"（尺寸/位置/唤回均未定义）。
- **修复**：合并为"鼠标离开 N 秒后隐藏"；定义半透明小条规格（200×36px、原位置右下角、30% 透明度、悬停 0.3s 展开）；补原型 7.5。
- **位置**：3.1、6.1、7.5

### B5. 删除任务的"回收站"在数据模型与 UI 中完全缺失 ✅ 已修复
- **问题**：6.3 说"移入回收站，支持撤销"，但 8.1 Task 无 `deletedAt`，8.5 无回收站文件，UI 无入口，撤销逻辑未定义。
- **修复**：Task 增 `deletedAt`；新增 6.8 回收站规范（软删除/撤销 Toast 5s/30 天清理/容量）；7.4 设置面板增回收站入口；新增 `TrashView.vue` + `UndoToast.vue` + `commands/trash.rs`。
- **位置**：6.3、6.8、7.4、8.1、9.x

---

## 三、重要问题（Major，已全部修复）

| 编号 | 问题 | 修复 |
|------|------|------|
| M1 | 透明度范围三处不一致（0-100 / 20-100 / 整数） | 统一 20%-100%，Config 存整数 20-100 |
| M2 | 功能映射表"通知调度"误指 SearchBar.vue | 改为 `NotificationHost.vue` + composables |
| M3 | 优先级表与功能/里程碑不一致 | 重排优先级表，搜索+快捷键入 P0；看板视图标注 MVP 不实现 |
| M4 | 快捷键冲突 + 全局/应用级未区分 | 重设计快捷键，明确应用级 + 冲突检测 |
| M5 | Tab.color 字段功能未定义 | 补充 Tab 颜色用途与分类颜色关系说明 |
| M6 | 数据模型含 P2 字段无版本说明 | 标注 MVP 空数组占位，新增 8.6 迁移机制 |
| M7 | 通知去重与跨重启逻辑缺失 | Task 增 `notifiedAt`，6.6 定义去重与补发策略 |
| M8 | MVP 范围对钉住条风险评估不足 | 阶段 1 首周技术 Spike，不通过则降级 P1 |

---

## 四、次要问题（Minor，已处理）

1. UUID 生成策略 → 明确 Rust `uuid` crate v4
2. 时区处理 → 统一存 UTC，展示按本地时区
3. 主题字段 → 标注 MVP 固定 light
4. Tab 与分类关系 → 补充正交说明
5. 单实例检查 → 纳入非功能需求
6. 开机自启 → 纳入非功能需求与配置
7. 备份保留策略 → 14 份滚动
8. `reminderTime` 与 `dueDate` 联动 → 补充说明
9. 附件类型 → 标注 MVP 仅 link 占位

---

## 五、开发就绪度评分

| 维度 | v1.1 评分 | v1.2 评分 | 说明 |
|------|-----------|-----------|------|
| 完整性 | 4 | 8 | 核心特性行为已定义，空/错/加载态部分仍可开发中补 |
| 一致性 | 3 | 8 | 窗口位置、透明度、优先级表、功能映射已对齐 |
| 可实现性 | 4 | 8 | 钉住条重定义可行，多窗口同步架构正确 |
| 清晰度 | 5 | 8 | 关键交互已明确，仍有少量细节待开发中细化 |
| 范围合理性 | 5 | 8 | MVP 范围收敛，钉住条有降级预案 |
| **综合分** | **4.2** | **8.0** | **达到 GO 标准** |

---

## 六、结论

v1.1 的 5 个阻断项与 8 个重要项已在 v1.2 全部修复，详见 PRD 第 14 章"开发就绪度评审记录"。

**当前状态：GO（可启动开发）。**

建议下一步：按 PRD 阶段 1，先做钉住条技术 Spike（1 周），通过后推进 P0 主线。
